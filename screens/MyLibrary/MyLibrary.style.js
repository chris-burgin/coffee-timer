import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    paddingLeft: 10,
    paddingRight: 10,
  },
  header: {
    marginTop: 15,
    fontSize: 40,
    fontWeight: "800",
  },
})

export default styles
