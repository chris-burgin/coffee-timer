import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
  },
  header: {
    fontSize: 40,
    fontWeight: "800",
    marginBottom: 10,
  },
})

export default styles
